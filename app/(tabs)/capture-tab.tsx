import React from 'react';
import { View } from 'react-native';

// Placeholder — questa tab non viene mai mostrata.
// La navigazione avviene tramite il FAB in _layout.tsx → /capture
export default function CaptureTab() {
  return <View />;
}
